
```sql
CREATE USER PERU1 IDENTIFIED BY "dPdirtjdnf1!";

-- CONNECT 및 RESOURCE 역할 부여
GRANT CONNECT TO PERU1;
GRANT RESOURCE TO PERU1;

-- 테이블스페이스 할당
ALTER USER PERU1 QUOTA UNLIMITED ON USERS;

ALTER TABLE peru.STAT_INFO
ADD CONSTRAINT UNIQUE_ROI_ID_SNAPSHOT_DATE UNIQUE (ROI_ID, SNAPSHOT_DATE);

CREATE TABLE "PERU"."ROI_SPATIAL"
( "ROI_ID" NUMBER,
"ROI_NAME" VARCHAR2(50) UNIQUE,
"POLYGON" "SDO_GEOMETRY",
"DAM" "SDO_GEOMETRY",
"RECT" "SDO_GEOMETRY",
PRIMARY KEY ("ROI_ID"));

CREATE SEQUENCE PERU.ROI_SPATIAL_SEQ INCREMENT BY 1 MINVALUE 0 MAXVALUE 9999999999999999999999999999 NOCYCLE NOCACHE NOORDER;

```